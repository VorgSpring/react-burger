import {
  NAME_FIELD_TYPE,
  EMAIL_FIELD_TYPE,
  PASSWORD_FIELD_TYPE,
  CODE_FIELD_TYPE,
} from './types';

export const LoginFieldNames = {
  EMAIL: EMAIL_FIELD_TYPE,
  PASSWORD: PASSWORD_FIELD_TYPE,
};

export const RegisterFieldNames = {
  NAME: NAME_FIELD_TYPE,
  EMAIL: EMAIL_FIELD_TYPE,
  PASSWORD: PASSWORD_FIELD_TYPE,
};

export const ForgotPasswordFieldNames = {
  EMAIL: EMAIL_FIELD_TYPE,
};

export const ResetPasswordFieldNames = {
  PASSWORD: PASSWORD_FIELD_TYPE,
  CODE: CODE_FIELD_TYPE,
};

export const ProfileFieldNames = {
  NAME: NAME_FIELD_TYPE,
  EMAIL: EMAIL_FIELD_TYPE,
  PASSWORD: PASSWORD_FIELD_TYPE,
};
